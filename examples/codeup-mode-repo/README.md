# Codeup Mode Sample

This fixture represents a Java/Maven repository that should use the Codeup CI
documentation path instead of GitHub Actions when initialized with:

```bash
claude-md-governance init --repo . --preset enterprise-java-codeup --ci codeup --config-change-mode warn --yes
```
